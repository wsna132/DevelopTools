这个Module主要用于各种主流第三方库封装成的工具，比如Glide（图片加载）等
不断完善中

通用目录结构说明：
    <模块名>.userEntrance       各个模块的使用入口，外部所有需要调用的方法及类都在这个包下面
    <模块名>.consts            各个模块配置默认值的地方，如加载图片失败时显示的通用默认图片

模块一：imageloader     图片加载模块

模块二：common     公用的工具模块